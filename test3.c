# include<stdio.h>
# include <string.h>
void lecture(char * s)
{
  printf("nom: ");
  scanf("%s",s);
}
int comparaison(char *s)
{ int v;
  if (strlen(s) > 10);
  v=1;
  return(v);
}
void main()
{
  int s=0;
  char t[20];
lecture(t);
  while ( strcmpr(t,"fin"))
{
  if (sterln(t)<20)
 if (comparaison(t))
 {
s += 1;
}
lecture(t);
}
}
